require "bundler/gem_tasks"
require "rspec/core/rake_task"
require "rake/extensiontask"

Rake::ExtensionTask.new "k2hash" do |ext|
  ext.lib_dir = "lib/k2hash"
end

#RSpec::Core::RakeTask.new(:spec)
#
#task :default => :spec
